import SwiftUI

/**
 * Main entry point for the Stodolist iOS application.
 * Uses SwiftUI App lifecycle to launch the Compose Multiplatform UI.
 */
@main
struct iOSApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
